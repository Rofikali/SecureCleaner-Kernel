# Update system
sudo pacman -Syu

# Install package
sudo pacman -S git

# Remove package
sudo pacman -R git

# Search package
pacman -Ss git

# List installed
pacman -Q


sudo pacman -S base-devel git neovim